# Läget

**Datum:** 2026-08-15
**Ersätter:** `PLAN_EFTER_GRANSKNING.md`, som nu är byggd i sin helhet

Alla åtta punkter i den planen är genomförda och mergade (PR #13–#21). Det här
dokumentet beskriver vad som finns, och vad som återstår.

---

## Vad sajten är

Nio sidor. Varje tal på dem räknas fram ur databasen — inga påståenden står
skrivna i koden.

| Sida | Bär |
|---|---|
| `/` | Fem fynd, det femte nu de 12 voteringar som kunnat sluta annorlunda |
| `/partier` | Åtta partier, vem de röstar oftast och minst med |
| `/partier/[parti]` | Samstämmighet, ämnesavvikelse mot egen normalnivå, ensam mot alla, frånvaro mot kammaren, reservationer, vinnande sida |
| `/amnen` | 16 ämnen, de två svaga hopfällda, exempel bakom `<details>` |
| `/samstammighet` | Matrisen, alla 28 par, metodrad om vad "samma linje" betyder |
| `/voteringar` | 2 587 beslut, paginerade 40 per sida, filter på ämne, riksmöte och fritext |
| `/voteringar/[id]` | Klarspråk, partiernas röster, debattens omfattning, originaltexterna |
| `/franvaro` | 13,4 % i display, alla fyra riksmöten, halvtidsfiltret utskrivet |
| `/metod` | Underlaget, sex definitioner, sju begränsningar, felanmälan, hyckleri-essän |

`/spanningar` 308:ar till `/metod#hyckleri`.

---

## Databasen

407 MB av gratisplanens 500. Åtta tabeller, elva materialiserade vyer, nio
vanliga vyer. Åtta av de nio vanliga är tillkomna under det här arbetet —
`utskottet_forlorade` fanns sedan tidigare. Ingen av dem kostar utrymme.

| Vy | Ger |
|---|---|
| `parti_utfall` | Hur ofta partiets linje låg på den vinnande sidan |
| `parti_disciplin` | Avlagda röster som avviker från egen partilinje |
| `parti_franvaro` | Frånvaro per parti och riksmöte |
| `parti_reservation` | Reservationer partiet stod bakom, och ensamt bakom |
| `votering_lista` | Platt lista, sorterbar på datum, pagineringsbar |
| `klartext_summering` | Modell och självskattad säkerhet |
| `retorik_summering` | Bedömningar per överensstämmelse |
| `betankande_debatt` | Anföranden och talare per parti |
| `utskottet_forlorade` | De två gånger nej-sidan vann |

### Tresekundersregeln

Står i `CLAUDE.md` sedan det här arbetet. `anon` har `statement_timeout = 3s`,
och supabase-js kastar inte — en avbruten fråga blir en nolla på sidan.
`parti_disciplin` skapades först som vy över `rost` (4 300 ms), materialiserades,
och visade sig sedan inte behöva läsa `rost` alls: samma tal går att aggregera
ur `parti_rost` på 18 ms. Att nå efter en cache var symtomet på att fel tabell
lästes.

---

## Vad som återstår

### NU

**1. Migrationerna kan inte återskapa databasen.**
Fem materialiserade vyer refereras av migrationerna men skapas aldrig av dem:
`parti_rost`, `jamn_votering`, `partisamstammighet`, `ledamot_franvaro`,
`riksmote_summering`. En färsk klon plus `supabase db reset` ger alltså ett
schema som inte fungerar, och felet syns först när en sida kraschar.

Det här är den allvarligaste posten på listan — allt annat är kosmetik i
jämförelse. En migration som skriver ned det befintliga vylagret, kontrollerad
genom att faktiskt köra den mot en ny branch-databas.

**2. Startsidan och voteringsdetaljen sväljer fortfarande fel.**
Sju sidor går genom `rader()` och `rakna()` i `lib/db.ts`, som läser `error` och
kastar. Två gör det inte: `app/page.tsx` (sex frågor) och
`app/voteringar/[id]/page.tsx` (tre av fyra). De renderar "—" eller tomma
listor när en fråga fallerar.

Ordningen spelar roll: `app/error.tsx` måste finnas **först**, annars byter
omkopplingen ett tyst fel mot en oformaterad 500-sida. Det finns varken
`error.tsx` eller `not-found.tsx` i dag.

**3. Två hårdkodade "99,9–100 %" kvar på startsidan.**
`app/page.tsx:222` och `:272`. Spannet räknas redan fram på `/metod`,
`/partier/[parti]`, `/samstammighet` och `/amnen` — startsidan är den sista
kopian. Kräver en fråga mot `partisamstammighet`.

### SNART

**4. Partilinjeregeln finns i fem exemplar.**
Samma `CASE`-sats står i `punkt_linje`, `partisamstammighet`, `jamn_votering`
och `parti_disciplin`, plus `partilinje()` i `lib/db.ts`. Migrationen
`20260815100000` bröt ut den till `punkt_linje` uttryckligen "så att regeln
finns på ett ställe", och sedan har den kopierats tillbaka.

Rätt nivå är en SQL-funktion `partilinje(ja, nej, avstar)` som alla fyra vyerna
anropar. Kräver att matvy-kedjan byggs om i rätt ordning — gör det som en egen
PR, inte som en bisak.

**5. Utrymmet räcker till ett riksmöte till.**
407 av 500 MB, ungefär 71 MB per riksmöte. Nästa import ryms; den därpå gör
det inte. Beslutet när det tar slut är Supabase Pro, 8 GB för 25 dollar i
månaden. `anforande.text` (43 MB) är medvetet sparat verifieringsunderlag och
rörs inte.

### KANDIDATER SOM BEHÖVER ETT BESLUT

**Sammanfattning av anförandena.** 56 177 anföranden ligger i databasen och
`/voteringar/[id]` visar nu debattens omfattning per parti. Att säga något om
*innehållet* kräver LLM-batch, alltså kostnadsuppskattning och valideringsrunda
enligt arbetsreglerna. Lager 3-försöket redovisas på `/metod` och höll inte —
en ny ansats behöver en annan fråga än "sagt mot röstat", inte en bättre prompt.

**Ledamotssidor.** Datan säger nej. 0,139 % av rösterna avviker från partiets
linje, så det finns ingen population att jämföra mellan. Det enda som
varierar mellan ledamöter är frånvaro, och den redovisas redan — med
förbehållet att skälen inte finns i öppna data.

**Delning.** Sajten har inga OG-bilder. Varje sida har nu `metadata` med titel
och beskrivning, så en delad länk ser vettig ut i text men saknar bild.

---

## Innan sajten kan driftsättas

`NEXT_PUBLIC_SUPABASE_ANON_KEY` måste sättas i Vercel. `lib/db.ts` kastar utan
den, och sidorna renderas vid bygget — **utan nyckeln failar bygget, inte bara
körningen**. Projekt-URL:en har en fallback och är valfri.

Sätt inte `SUPABASE_SECRET_KEY`, `SUPABASE_SERVICE_ROLE_KEY` eller
`OPENAI_API_KEY` i Vercel. De används bara av ETL- och batchskripten, som körs
lokalt, och service role-nyckeln kringgår RLS.

---

## Vad som inte är kontrollerat

Ärlighet om täckningen i det här arbetet:

- **Ingen produktionsbyggnad har körts.** `npx tsc --noEmit` är rent, och alla
  rutter svarar 200 mot utvecklingsservern, men `npm run build` har inte körts
  (arbetsregeln säger att den inte får köras medan dev-servern lever).
- **Tangentbordsnavigering är inte genomgången.** Formspråkets checklista tar
  upp den; jag har verifierat kontrast, mörkt läge och 375 px men inte
  fokusordning eller synlig fokusmarkering.
- **Desktop-vyn är mätt, inte sedd i sin helhet.** Skärmdumpsverktyget svarade
  intermittent med tom bild under arbetet. Layouten är verifierad genom
  DOM-mätningar (bredder, radhöjder, horisontell scroll) och genom
  skärmdumpar på 375 px, som fungerade genomgående.
